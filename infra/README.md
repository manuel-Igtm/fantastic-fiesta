# Infrastructure Workspace

This directory is reserved for infrastructure as code and deployment tooling.

Planned structure:

- `terraform/` for cloud resources and IAM policy definitions.
- `k8s/` for Kubernetes manifests and Helm values.
- `scripts/` for operational bootstrap helpers.
